<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.example.demo.dao.FormClounmnInfoDao">
 
  <select id="findList" resultType="com.example.demo.entitys.FormClounmnInfo">
   select form_id,column_id,column_name,form_name,column_type from form_clounmn_info 
   where form_id=#{form_id}; 
  </select>
  <!-- <select id="findAll" resultType="com.example.demo.entitys.ClounmnInfo">
   select deptno,dname,db_source from dept; 
  </select>
  <insert id="addDept" parameterType="com.example.demo.entitys.ClounmnInfo">
   INSERT INTO dept(dname,db_source) VALUES(#{dname},DATABASE());
  </insert>-->
  
</mapper>
 

